import { RoleFilterDirective } from './role-filter.directive';

describe('RoleFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleFilterDirective();
    expect(directive).toBeTruthy();
  });
});
